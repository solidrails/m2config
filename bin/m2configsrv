#!/usr/bin/env ruby

begin
  require "m2config"
rescue LoadError
  lib_path = File.expand_path('../../lib', __FILE__)
  $:.unshift(lib_path)
  require "m2config"
end
require 'm2config/handler'

M2::launch_api_server ARGV[0]